const inquirer = require('inquirer');

require('console.table');

const questions = [
    {
        type: 'list',
        name: ''
    }
];

async function something() {
    inquirer
    .prompt([
        
    ])
};